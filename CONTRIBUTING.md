# راهنمای مشارکت

از گزارش خطا، پیشنهاد و Pull Request استقبال می‌شود. هدف اصلی، حفظ یک قالب پایدار، قابل‌بازتولید و سازگار با نیازهای دانشجویان دانشکده علوم ریاضی است.

## گزارش خطا

فرم Bug report در بخش Issues شامل اطلاعات لازم برای بازتولید است. پیش از بارگذاری فایل، همه اطلاعات شخصی، امضاها، شماره‌های دانشجویی و داده‌های محرمانه حذف شوند.

## Pull Request

هر Pull Request باید:

- یک مسئله مشخص و محدود را حل کند؛
- توضیح دهد چه چیزی و چرا تغییر کرده است؛
- هر دو قالب را در صورت اشتراک تغییر، همگام نگه دارد؛
- در حالت `bib` و `manual` آزمون شده باشد؛
- خطا، هشدار، ارجاع ناقص، `Overfull` یا `Underfull` ایجاد نکند؛
- مجوز و منشأ هر فونت، تصویر یا وابستگی جدید را ثبت کند.

## تغییرات بخش اداری

تغییر در `Attributes`، نشان‌ها یا تصاویر رسمی باید در Pull Request جداگانه انجام شود و به شیوه‌نامه یا منبع دانشگاهی معتبر ارجاع دهد. اصلاح فصل‌های نمونه نباید به‌طور ناخواسته بخش اداری را تغییر دهد.

## سبک محتوا

- متن فصل‌های نمونه علمی، خنثی و غیرشخصی باشد.
- داده‌ها و اعداد نمونه به‌روشنی ساختگی یا نمایشی معرفی شوند.
- ادعای پژوهشی واقعی بدون منبع وارد قالب نشود.
- نام‌گذاری برچسب‌ها یکتا و معنادار باشد.

## آزمون محلی

در پوشه هر مقطع:

```powershell
.\build.ps1
```

برای کنترل حالت دستی، مقدار `\TMUBibliographyMode` به‌صورت موقت روی `manual` قرار می‌گیرد و پس از آزمون به مقدار پیش‌فرض بازگردانده می‌شود.

## Commit

پیام commit کوتاه و روشن باشد، مانند:

```text
fix: correct Persian bibliography direction
docs: clarify Overleaf setup
feat: add a theorem environment example
```
